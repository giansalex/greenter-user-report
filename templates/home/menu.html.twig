<nav class="navbar navbar-inverse">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#menu-nav">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand">XML to PDF</a>
        </div>
        <div class="collapse navbar-collapse" id="menu-nav">
            <ul class="nav navbar-nav navbar-right" style="font-size: 18px">
                {% if user %}
                <li>
                    <a href="#">{{ user.email }}</a>
                </li>
                <li>
                    <a href="{{ path_for('index') }}" title="Upload">
                        <i class="fa fa-upload"></i> Upload
                    </a>
                </li>
                <li>
                    <a href="{{ path_for('setting') }}" title="config">
                        <i class="fa fa-cog"></i> Configuracion
                    </a>
                </li>
                <li>
                    <a href="{{ path_for('logout') }}" title="Logout">
                        <i class="fa fa-toggle-on"></i> Salir
                    </a>
                </li>
                {% else %}
                <li>
                    <a href="{{ path_for('login') }}" title="Login">
                        <i class="fa fa-toggle-off"></i> Login
                    </a>
                </li>
                {% endif %}
            </ul>
        </div>

    </div>
</nav>